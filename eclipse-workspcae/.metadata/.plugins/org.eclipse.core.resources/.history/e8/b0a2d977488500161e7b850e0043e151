package com.bank;

public class Teller implements Runnable{

	Bank b;
	public Teller(Bank bank) {
		b = bank;
	}
	@Override
	public void run() {
		
		int accto = (int)Math.random()%10;
		int accfrom = (int)Math.random()%10;
		
	}

	
}
